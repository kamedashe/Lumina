# Node / JS
node_modules
dist
.env
.DS_Store

# Tauri / Rust
src-tauri/target/
src-tauri/gen/
src-tauri/schemas/

# Local data
src-tauri/*.db
test.txt

# IDEs
.vscode
.idea
*.swp

# Python sidecar
__pycache__/
*.pyc
.pytest_cache/

# Eval-индексы: пересоздаются из корпуса командой
# `python evals/index_corpus.py --config <config>` (31 МБ бинарных SQLite).
# Результаты прогонов в evals/results/ при этом версионируются — они и есть
# доказательство измерений.
evals/index/
